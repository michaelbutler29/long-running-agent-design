[
  {
    "name": "get_customer",
    "description": "Returns the customer profile for a given customer ID, including name, contact details, addresses, and account status.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "customer_id": {
          "type": "string",
          "description": "The customer identifier (e.g. CUST-001)."
        }
      },
      "required": ["customer_id"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "customer_id": {"type": "string"},
        "first_name": {"type": "string"},
        "last_name": {"type": "string"},
        "email": {"type": "string"},
        "phone": {"type": "string"},
        "address": {"type": "string"},
        "billing_address": {"type": "string"},
        "status": {"type": "string"}
      }
    }
  },
  {
    "name": "get_order",
    "description": "Returns order details for a given order ID, including items, total, date, status, shipping address, and a human-readable details field describing any notable conditions (split shipments, backorders, item mismatches, signature requirements).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "order_id": {
          "type": "string",
          "description": "The order identifier (e.g. ORD-1001)."
        }
      },
      "required": ["order_id"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "order_id": {"type": "string"},
        "customer_id": {"type": "string"},
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": {"type": "string"},
              "price": {"type": "number"}
            }
          }
        },
        "total": {"type": "number"},
        "order_date": {"type": "string"},
        "status": {"type": "string"},
        "shipping_address": {"type": "string"},
        "details": {"type": "string"}
      }
    }
  },
  {
    "name": "verify_identity",
    "description": "Runs identity verification for a customer. Returns whether the customer passed verification. Must be called before any write operation.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "customer_id": {
          "type": "string",
          "description": "The customer identifier."
        }
      },
      "required": ["customer_id"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "customer_id": {"type": "string"},
        "verified": {"type": "boolean"},
        "method": {"type": "string"}
      }
    }
  },
  {
    "name": "update_customer_field",
    "description": "Updates a customer profile field (email, phone, address, or billing_address). Modifies PII — requires that customer_verified is true.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "customer_id": {
          "type": "string",
          "description": "The customer identifier."
        },
        "field": {
          "type": "string",
          "description": "The field to update: email, phone, address, or billing_address."
        },
        "value": {
          "type": "string",
          "description": "The new value for the field."
        },
        "customer_verified": {
          "type": "boolean",
          "description": "Whether the customer's identity has been verified. Must be true."
        }
      },
      "required": ["customer_id", "field", "value", "customer_verified"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "customer_id": {"type": "string"},
        "field": {"type": "string"},
        "value": {"type": "string"},
        "status": {"type": "string"}
      }
    }
  },
  {
    "name": "check_refund_eligibility",
    "description": "Checks whether an order is eligible for a refund. Eligible cases: (1) delivered within the 30-day return window (standard return), or (2) significantly delayed or never delivered — eligible for a cancellation refund.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "order_id": {
          "type": "string",
          "description": "The order identifier."
        }
      },
      "required": ["order_id"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "order_id": {"type": "string"},
        "eligible": {"type": "boolean"},
        "reason": {"type": "string"},
        "refund_type": {"type": "string"},
        "refund_amount": {"type": "number"}
      }
    }
  },
  {
    "name": "process_refund",
    "description": "Processes a refund for an eligible order. Requires both customer verification and confirmed eligibility.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "order_id": {
          "type": "string",
          "description": "The order identifier."
        },
        "customer_id": {
          "type": "string",
          "description": "The customer identifier."
        },
        "customer_verified": {
          "type": "boolean",
          "description": "Whether the customer's identity has been verified. Must be true."
        },
        "refund_eligible": {
          "type": "boolean",
          "description": "Whether the order has been confirmed eligible for refund. Must be true."
        }
      },
      "required": ["order_id", "customer_id", "customer_verified", "refund_eligible"]
    },
    "outputSchema": {
      "type": "object",
      "properties": {
        "order_id": {"type": "string"},
        "refund_amount": {"type": "number"},
        "status": {"type": "string"},
        "expected_days": {"type": "number"}
      }
    }
  }
]